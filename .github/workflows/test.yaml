name: Test

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  contracts:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v9.0.0
        with:
          version: "0.11.21"
          python-version: "3.11.9"
          enable-cache: true

      - name: Install dependencies
        run: uv sync --locked

      - name: Compile contracts
        run: uv run --locked vesting-escrow-compile | grep '^compiled ' | tee checkout-compile.txt

      - name: Run functional tests
        run: uv run --locked pytest tests/functional/ --gas-profile

      - name: Run property tests
        run: uv run --locked pytest tests/integration/

      - name: Verify frozen release sources and bytecode
        run: uv run --locked pytest tests/test_release_manifest.py

      - name: Build package
        run: uv build

      - name: Smoke test installed wheel
        run: |
          uvx --from ./dist/yearn_vesting_escrow-0.4.0-py3-none-any.whl vesting-escrow-compile | grep '^compiled ' | tee wheel-compile.txt
          diff -u checkout-compile.txt wheel-compile.txt
          uvx --from ./dist/yearn_vesting_escrow-0.4.0-py3-none-any.whl vesting-escrow-deploy
